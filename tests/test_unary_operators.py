# Test unary operators: +, -, ~, not

# Unary plus
positive = +x
pos_num = +42

# Unary minus
negative = -x
neg_num = -42

# Bitwise NOT
inverted = ~x
bit_not = ~255

# Logical NOT
not_true = not True
not_false = not False
not_condition = not (x > 5)

# Combined unary operations
double_neg = -(-x)
complex_unary = ~(-x)
not_and_unary = not (-x > 0)

# Unary in expressions
result = -x + y
computed = ~(x & y)
condition = not (x == 0)


