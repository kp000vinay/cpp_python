# Test bitwise operators: |, &, ^, ~, <<, >>

# Test bitwise OR
bit_or = x | y
result = a | b | c

# Test bitwise AND
bit_and = x & y
result = a & b & c

# Test bitwise XOR
bit_xor = x ^ y
result = a ^ b ^ c

# Test bitwise NOT
bit_not = ~x
negated = ~(x & y)

# Test left shift
left_shift = x << 2
shifted = y << 4

# Test right shift
right_shift = x >> 2
shifted = y >> 4

# Test combined bitwise operations
combined = (x | y) & z
complex_op = (a << 2) | (b >> 1)
nested = ~(x & y) | z


