# Test union types (Python 3.10+ syntax - may not be supported)
# Tests: Union type syntax with | operator
# NOTE: This test may fail if | operator not supported in type context

def process(value: int) -> int:
    return value

x: str = "test"
