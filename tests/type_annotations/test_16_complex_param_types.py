# Test complex parameter types
# Tests: Subscript types in parameters

def process(items: list[int], mapping: dict[str]):
    pass

def transform(data: list[tuple]):
    pass

def nested(config: dict[list]):
    pass

def handle(values: list[str], counts: dict[int]):
    pass
