# Test nested type expressions
# Tests: Nesting of type expressions (single parameter subscripts)

data: dict[list] = {}

def process(items: list[dict]) -> dict[list]:
    return {}

config: dict[dict] = {}

def transform(nested: list[list]) -> list[list]:
    return []
